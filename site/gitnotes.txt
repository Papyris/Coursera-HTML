cd 		change directory
cd .. 		go back one parent node
cd ../..	go back two
mkdir		create directory
touch 		create file
ls		list items in directory
pwd		current directory


When in git directory folder 

git status
git add		add files to be tracked
git commit -m "comment"		This needs to be included to ensure all files go
git push 	trigger the updates

browser-sync --http --server (sync browser with directory to save reloading)
